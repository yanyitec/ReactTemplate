{    
    "less.compile": {
        "compress":  false,  // true => remove surplus whitespace
        "sourceMap": true,  // true => generate source maps (.css.map files)
        "out":       "${workspaceRoot}/wwwroot/", // false => DON'T output .css files (overridable per-file, see below)
    }
}